{
    "scripts" : {
        "listFiles" : "ls -la",
        "createFile" : "echo `date +%Y-%m-%d:%H:%M:%S` > date.txt",
        "runAll": [
            "@listFiles",
            "@createFile"
        ]
    }
}